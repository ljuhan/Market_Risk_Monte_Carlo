name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install -r requirements-lock.txt
      - name: Run tests
        run: python -m pytest -v
      - name: Regenerate results from frozen data
        run: python scripts/run_analysis.py --output /tmp/risk-reproduced
      - name: Check numerical reproducibility
        run: python scripts/check_results.py /tmp/risk-reproduced/summary.json
